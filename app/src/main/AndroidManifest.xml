<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nexvis.nexretail">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nexretail">
        <activity android:name=".view.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.Home.HomeActivity"/>
        <activity android:name=".view.activity.Book.BookingActivity"/>
        <activity android:name=".view.activity.Serve.ServeActivity"/>
        <activity android:name=".view.activity.Room.RoomActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".view.activity.Kitchen.KitchenActivity"/>
        <activity android:name=".view.activity.OpenBill.OpenBillActivity"/>
        <activity android:name=".view.activity.Book.BookNowActivity"/>
    </application>

</manifest>